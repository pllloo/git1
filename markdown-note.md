# Markdown 速查笔记

> 面向 GitHub + VSCode 场景的 Markdown 常用语法速查，每条语法都配了示例。
> 写文档时在 VSCode 按 `Ctrl+Shift+V` 实时预览，养成习惯。

---

## 〇、三条铁律（先记住这个）

1. **标记符号后面必须跟一个空格**：`# 标题` ✓，`#标题` ✗（GitHub 不渲染）
2. **段落之间用空行隔开**
3. **写完先预览再提交**：VSCode 里 `Ctrl+Shift+V`；GitHub 网页上长文档会自动生成目录

---

## 一、标题

| 语法 | 效果 |
|------|------|
| `# 一级标题` | 最大的标题，一个文件建议只用一次 |
| `## 二级标题` | 章节标题 |
| `### 三级标题` | 小节标题 |
| `#### 四级标题` | 更小的节，一般用到三四级就够 |

---

## 二、文本样式

| 语法 | 效果 |
|------|------|
| `**粗体**` | **粗体** |
| `*斜体*` | *斜体* |
| `~~删除线~~` | ~~删除线~~ |
| `` `行内代码` `` | `行内代码` |
| `**粗体加*斜体*嵌套**` | **粗体加*斜体*嵌套** |

---

## 三、列表

无序列表用 `-`，有序列表用数字加点，列表项之间不用空行：

- 无序列表项 1
- 无序列表项 2
  - 嵌套：前面缩进两个空格
  - 再嵌套一层

1. 有序列表项 1
2. 有序列表项 2
   1. 嵌套有序项

**任务列表**（学习计划神器）：

- [ ] 未完成的任务
- [x] 已完成的任务

---

## 四、引用块

> 引用块用 `>` 开头，适合放重点提醒、他人观点、学习提示。
>
> 多段引用：在空行上也写一个 `>`。

---

## 五、代码块

行内代码用反引号：`git push origin master`

代码块用三反引号包裹，**开头写语言名**才有语法高亮：

```python
def hello():
    print("Hello, Agent!")
```

```bash
git add .
git commit -m "说明"
```

---

## 六、链接与图片

| 语法 | 用途 |
|------|------|
| `[链接文字](https://github.com)` | 外链 |
| `[跳转到学习笔记1](学习笔记1.md)` | 链接同仓库其他笔记（相对路径） |
| `[页内跳转](#章节标题)` | 页内锚点（GitHub 会去掉标题中的标点，空格变 `-`） |
| `![图片说明](images/pic.png)` | 插入图片（用相对路径） |

---

## 七、表格

表格前后要空行；`|---|` 是表头和内容的分隔线；冒号控制对齐：

| 左对齐 | 居中 | 右对齐 |
|:-------|:----:|-------:|
| 内容 | 内容 | 内容 |

---

## 八、分割线与换行

`---` 单独一行 = 分割线（前面要有空行）：

---

**换行规则**：
- 段落之间：空一行
- 段落内部强制换行：行尾加两个空格再回车（更推荐直接空行分段）

---

## 九、转义

想在文本里显示符号本身（而不是触发语法），前面加 `\`：

`\# 这不是标题` → \# 这不是标题

`\* 这不是列表` → \* 这不是列表

---

## 十、折叠块（GitHub 支持）

长内容（如报错日志）可以折叠起来：

<details>
<summary>点击展开：示例报错</summary>

```
Traceback (most recent call last):
  ...
```

</details>

---

## 十一、常用模板：学习笔记

````markdown
# 章节/主题名

> 一句话说明这篇笔记讲什么。

## 核心概念

- 要点 1
- 要点 2

## 代码示例

```python
# 这里放代码
```

## 我的理解 / 踩坑记录

> 用自己的话复述一遍，讲不清楚 = 没学会。

## 待办

- [ ] 还没搞懂的问题
- [x] 已解决：xxx（怎么解决的）
````

---

## 速记口诀

> **标记要空格，段落要空行，代码标语言，写完先预览。**
